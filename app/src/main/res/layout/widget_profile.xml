<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="14dp"
    android:background="@drawable/widget_background">

    <!-- Header row: CF logo + App label + Refresh button -->
    <ImageView
        android:id="@+id/widget_logo"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/cf_logo"
        android:contentDescription="@string/app_name" />

    <TextView
        android:id="@+id/widget_app_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/widget_logo"
        android:layout_centerVertical="true"
        android:layout_alignTop="@id/widget_logo"
        android:layout_alignBottom="@id/widget_logo"
        android:gravity="center_vertical"
        android:layout_marginStart="6dp"
        android:text="@string/widget_app_name"
        android:textSize="11sp"
        android:textColor="#99FFFFFF" />

    <ImageView
        android:id="@+id/widget_refresh"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:padding="2dp"
        android:src="@drawable/ic_refresh"
        android:contentDescription="@string/widget_refresh_desc" />

    <!-- Username handle -->
    <TextView
        android:id="@+id/widget_handle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_logo"
        android:layout_alignParentStart="true"
        android:layout_marginTop="10dp"
        android:textSize="17sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="" />

    <!-- Rank badge -->
    <TextView
        android:id="@+id/widget_rank"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_handle"
        android:layout_alignParentStart="true"
        android:layout_marginTop="4dp"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:background="@drawable/widget_rank_badge_bg"
        android:textSize="11sp"
        android:textStyle="bold"
        android:textColor="#80FFFFFF"
        android:maxLines="1"
        android:text="" />

    <!-- Rating stats row -->
    <LinearLayout
        android:id="@+id/widget_stats_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_rank"
        android:layout_marginTop="8dp"
        android:orientation="horizontal">

        <!-- Current Rating -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/widget_rating_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_rating_label"
                android:textSize="10sp"
                android:textColor="#80FFFFFF" />

            <TextView
                android:id="@+id/widget_rating"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:text="" />
        </LinearLayout>

        <!-- Max Rating -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/widget_max_rating_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_max_rating_label"
                android:textSize="10sp"
                android:textColor="#80FFFFFF" />

            <TextView
                android:id="@+id/widget_max_rating"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:text="" />
        </LinearLayout>

    </LinearLayout>

</RelativeLayout>